[package]
name = "generator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0.164", features = ["derive"] }

pdfium-render = "0.8.5"
genpdf = { path = "../genpdf-rs-patched" }
